# AGENTS

This file is the canonical engineering source of truth for this repository.

- For Codex: load this file first.
- For Claude CLI and Gemini CLI: their tool-specific entry files only point here.

## Project Summary

{{description}}

The npm package name is `{{npm_scope}}/{{name}}`.
Installed CLI command: `{{name}}`.

## Runtime Architecture

- [`src/cli.mjs`](src/cli.mjs) — command routing and core logic
- [`src/meta.mjs`](src/meta.mjs) — reads name and version from package.json

## Non-Negotiable Invariants

(fill in key behavioral constraints here)

## Verification

```bash
npm test
```

## Release

See [`docs/npm-publish-sop.md`](docs/npm-publish-sop.md) for the full publish flow.

Semver policy:
- `patch`: docs, metadata, tests, or backward-compatible fixes
- `minor`: backward-compatible user-visible features
- `major`: breaking CLI or runtime behavior changes

Do not bump the version for docs-only changes by default.

## Change Rules

- Keep the code dependency-free unless there is a strong reason not to.
- If behavior changes, update this file first or in the same change.
